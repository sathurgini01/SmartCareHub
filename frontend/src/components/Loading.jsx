import React from 'react';

const Loading = () => (
  <div className="spinner" style={{ minHeight: '100px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
    <div className="spinner"></div>
  </div>
);

export default Loading;
