# ══════════════════════════════════════════════════════════════════════════════
#  Patient Service — MongoDB
#  PersistentVolumeClaim + Deployment + ClusterIP Service
# ══════════════════════════════════════════════════════════════════════════════

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: patient-db-pvc
  namespace: smartcarehub
  labels:
    app: patient-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient-db
  namespace: smartcarehub
  labels:
    app: patient-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: patient-db
  template:
    metadata:
      labels:
        app: patient-db
    spec:
      containers:
        - name: patient-db
          image: mongo:7.0
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_DATABASE
              value: patient-service
          volumeMounts:
            - name: patient-db-storage
              mountPath: /data/db
          resources:
            requests:
              memory: "256Mi"
              cpu:    "250m"
            limits:
              memory: "512Mi"
              cpu:    "500m"
          livenessProbe:
            exec:
              command: ["mongosh", "--eval", "db.adminCommand('ping')"]
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command: ["mongosh", "--eval", "db.adminCommand('ping')"]
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
      volumes:
        - name: patient-db-storage
          persistentVolumeClaim:
            claimName: patient-db-pvc

---

apiVersion: v1
kind: Service
metadata:
  name: patient-db-service
  namespace: smartcarehub
  labels:
    app: patient-db
spec:
  type: ClusterIP
  selector:
    app: patient-db
  ports:
    - port: 27017
      targetPort: 27017
